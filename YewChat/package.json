{
    "name": "yewchat",
    "version": "1.0.0",
    "description": "",
    "main": "bootstrap.js",
    "scripts": {
        "wasm:build": "rimraf pkg && wasm-pack build --target web --dev --out-dir pkg --out-name yewchat -- --features wee_alloc",
        "build": "rimraf dist && npm run wasm:build && webpack",
        "start": "rimraf dist && npm run wasm:build && webpack-dev-server --open -d eval --port 0",
        "test": "cargo test && wasm-pack test --headless"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@wasm-tool/wasm-pack-plugin": "^1.6.0",
        "copy-webpack-plugin": "^12.0.2",
        "rimraf": "^5.0.7",
        "webpack": "^5.102.1",
        "webpack-cli": "^5.1.4",
        "webpack-dev-server": "^5.2.2"
    }
}
